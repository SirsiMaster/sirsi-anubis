{
  "_comment": "ADR-024 Decision 4: the router supervisor hook is now wired user-scope in ~/.claude/settings.json (authoritative, default-on for home-dir sessions). Project-scope wiring was retired here to avoid double-firing. The hook SOURCE lives at .claude/hooks/router_inbox_check.py and is deployed to ~/.claude/hooks/.",
  "permissions": {
    "allow": [
      "Bash(*)",
      "Edit(*)",
      "Write(*)",
      "Read(*)",
      "Glob(*)",
      "Grep(*)"
    ]
  }
}
